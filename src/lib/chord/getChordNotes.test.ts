import { describe, it, expect } from 'vitest';
import { getChordNotes } from './getChordNotes';

describe(getChordNotes, () => {
	it('should handle simple chords', () => {
		// TODO: Write tests
		expect(1).toEqual(1);
	});
});
